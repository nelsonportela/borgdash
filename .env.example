# Environment Variables Example
# Copy this file to .env and modify the values as needed
# Most users won't need to change anything!

# Database Configuration
DATABASE_URL=sqlite:///./data/borgdash.db

# Application Environment
ENVIRONMENT=production

# Note: No need to configure ports, URLs, or CORS!
# BorgDash uses a single container serving both frontend and API
# Access via: http://your-server:3157